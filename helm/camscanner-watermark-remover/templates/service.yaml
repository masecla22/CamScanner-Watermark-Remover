apiVersion: v1
kind: Service
metadata:
  name: {{ include "camscanner-watermark-remover.fullname" . }}
  labels:
    {{- include "camscanner-watermark-remover.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "camscanner-watermark-remover.selectorLabels" . | nindent 4 }}
